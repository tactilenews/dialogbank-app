name: "Project Setup"
description: "Standard setup including mise-managed tools (Node, pnpm) and dependencies"
runs:
  using: "composite"
  steps:
    - name: Install mise-managed tools
      uses: jdx/mise-action@v4

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
